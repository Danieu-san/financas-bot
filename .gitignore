#gitignore
# Dependências do Node (pasta gigante e desnecessária)
node_modules/

# Arquivos de ambiente com senhas e chaves (MUITO IMPORTANTE)
.env
credentials.json
token.json
*.pem
*.key

# Pastas de cache e sessão do whatsapp-web.js (contém dados de login)
.wwebjs_auth/
.wwebjs_cache/

# Estado/runtime local
.e2e/
data/
logs/
backups/
state_store.json
state_store.tmp
state_store.replay.json
state_store.replay.tmp
